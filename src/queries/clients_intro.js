const metric = {
  id: 'clients_intro',
  name: ``,
  description: '',
  chartType: 'text',
  content: `> **Client diversity:** isn't just a **best practice** — it's **critical for network resilience**.`
};

export default metric;