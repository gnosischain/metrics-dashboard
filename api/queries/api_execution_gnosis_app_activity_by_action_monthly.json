{
  "id": "api_execution_gnosis_app_activity_by_action_monthly",
  "query": "SELECT toDate(month) AS date, activity_kind AS label, n_events, n_users\r\n    FROM dbt.api_execution_gnosis_app_activity_by_action_monthly\r\n    WHERE activity_kind != 'onboard'\r\n    ORDER BY date ASC, label ASC"
}